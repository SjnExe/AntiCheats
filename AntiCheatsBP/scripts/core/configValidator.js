/**
 * @file Configuration Validator (STUBBED FOR DIAGNOSTICS)
 * @module AntiCheatsBP/scripts/core/configValidator
 * This is a temporary stub file to isolate a TypeError during initialization.
 * All functions are replaced with simple stubs that return no errors.
 */

/**
 * STUB: Validates the main configuration object.
 * @returns {string[]} An empty array.
 */
export function validateMainConfig(config, actionProfiles, knownCommands, commandAliasesMap) {
    // console.log("[configValidator STUB] validateMainConfig called");
    return [];
}

/**
 * STUB: Validates the checkActionProfiles object.
 * @returns {string[]} An empty array.
 */
export function validateActionProfiles(actionProfiles) {
    // console.log("[configValidator STUB] validateActionProfiles called");
    return [];
}

/**
 * STUB: Validates the automodConfig object.
 * @returns {string[]} An empty array.
 */
export function validateAutoModConfig(autoModConfig, actionProfiles) {
    // console.log("[configValidator STUB] validateAutoModConfig called");
    return [];
}

/**
 * STUB: Validates the ranksConfig object.
 * @returns {string[]} An empty array.
 */
export function validateRanksConfig(ranksConfig, mainConfigOwnerName, mainConfigAdminTag) {
    // console.log("[configValidator STUB] validateRanksConfig called");
    return [];
}
